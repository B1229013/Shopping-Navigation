package com.example.shopping.ui.theme

import androidx.compose.ui.graphics.Color

// ── Cinematic Dark Palette ──────────────────────────────────
// A warm-noir system: deep backgrounds recede, gold draws the eye.

// Backgrounds — layered depth
val Noir         = Color(0xFF09090B)   // deepest void
val SurfaceDim   = Color(0xFF131316)   // primary surface
val SurfaceBase  = Color(0xFF1A1A1F)   // cards, containers
val SurfaceBright= Color(0xFF242430)   // hover / active states
val SurfaceGlow  = Color(0xFF2E2E3A)   // elevated elements

// Text
val TextPrimary  = Color(0xFFF2F0E8)   // warm off-white
val TextSecondary= Color(0xFF8C8C96)   // muted mid-gray
val TextTertiary = Color(0xFF58585F)   // subtle hints
val TextDisabled = Color(0xFF3A3A42)   // disabled

// Accent — cinematic gold
val Gold         = Color(0xFFCAAE6E)   // primary accent
val GoldBright   = Color(0xFFE4D4A0)   // active / hover
val GoldDim      = Color(0xFF8A7848)   // subtle tint
val GoldSurface  = Color(0xFF1E1B14)   // gold-tinted surface

// Semantic
val Danger       = Color(0xFFCF4B4B)   // destructive
val DangerDim    = Color(0xFF3A1C1C)   // danger background
val Success      = Color(0xFF4BBF6E)   // completed / positive
val SuccessDim   = Color(0xFF152A1C)   // success background
val Info         = Color(0xFF5B8DEF)   // informational
val InfoDim      = Color(0xFF141D2E)   // info background

// Dividers & borders
val Border       = Color(0xFF232329)   // subtle border
val BorderBright = Color(0xFF33333D)   // stronger border

// Chart palette — four distinct hues for budget categories
val ChartBlue    = Color(0xFF5B8DEF)
val ChartGreen   = Color(0xFF4BBF6E)
val ChartAmber   = Color(0xFFD4A843)
val ChartViolet  = Color(0xFF9B6ED4)
