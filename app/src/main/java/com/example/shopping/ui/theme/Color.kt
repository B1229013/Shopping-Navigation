package com.example.shopping.ui.theme

import androidx.compose.ui.graphics.Color

// ── Cinematic Dark Palette ──────────────────────────────────
// Deep noir foundation with fresh green accents — inspired by
// premium food-tech aesthetics. The dark recedes, green guides.

// Backgrounds — layered depth
val Noir         = Color(0xFF060608)   // deepest void
val SurfaceDim   = Color(0xFF101012)   // primary surface
val SurfaceBase  = Color(0xFF161618)   // cards, containers
val SurfaceBright= Color(0xFF1E1E22)   // hover / active states
val SurfaceGlow  = Color(0xFF262630)   // elevated elements

// Text
val TextPrimary  = Color(0xFFF5F5F7)   // crisp white
val TextSecondary= Color(0xFF8E8E93)   // muted mid-gray
val TextTertiary = Color(0xFF555560)   // subtle hints
val TextDisabled = Color(0xFF38383D)   // disabled

// Accent — fresh green (cinematic food-tech feel)
val Gold         = Color(0xFF4ADE80)   // primary accent (green)
val GoldBright   = Color(0xFF86EFAC)   // active / hover
val GoldDim      = Color(0xFF22804A)   // subtle tint
val GoldSurface  = Color(0xFF0D1A12)   // green-tinted surface

// Semantic
val Danger       = Color(0xFFFF6B6B)   // destructive
val DangerDim    = Color(0xFF2D1515)   // danger background
val Success      = Color(0xFF4ADE80)   // completed / positive
val SuccessDim   = Color(0xFF0D1A12)   // success background
val Info         = Color(0xFF60A5FA)   // informational
val InfoDim      = Color(0xFF0F1A2E)   // info background

// Dividers & borders
val Border       = Color(0xFF1E1E24)   // subtle border
val BorderBright = Color(0xFF2C2C34)   // stronger border

// Chart palette — four distinct hues for budget categories
val ChartBlue    = Color(0xFF60A5FA)
val ChartGreen   = Color(0xFF4ADE80)
val ChartAmber   = Color(0xFFFBBF24)
val ChartViolet  = Color(0xFFA78BFA)
